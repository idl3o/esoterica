# HANDOFF — Esoterica Restructure

**For:** Claude Code session in VS Code, repo `idl3o/esoterica`
**From:** Planning session with Sam (claude.ai), July 2026
**Companion files:** `RESTRUCTURE.md` (full rationale + tree), `README-draft.md` (replacement README)
**Authority:** Sam has approved the plan. Execute commits 1–2 directly on `main`. Commits 3–4 go on branch `restructure/corpus-apparatus`; do NOT merge to main — Sam signs off after preview deploy. Never force-push.

---

## Context in three sentences

The repo is a ~300-document esoteric/contemplative corpus plus the site, services, and tooling that serve it, all interleaved at root with committed build artifacts and deployment-status files. Goal: corpus-forward layout (`corpus/` | `apparatus/` | `docs/`), generated indices out of version control, root reduced to threshold documents. The new README (in the anthology's absent-narrator register) replaces the current one at the end.

## Pre-flight

1. `git status` clean; `git pull`. Note current default branch (assume `main`; verify).
2. Confirm Vercel builds from this repo — read `vercel.json` and `package.json` scripts before moving anything; record every path referenced.
3. Grep for path constants before each move: `grep -rn "synthesis/\|site/\|library-index\|synthesis-index\|extractions-index" --include="*.js" --include="*.ts" --include="*.json" --include="*.sh" --include="*.html" .` — build a checklist of files to patch in commit 3.
4. Check whether `constellation/constellation.json` is generated by `build-synthesis-index.js` or hand-curated (inspect the script). Generated → gitignore set; curated → moves to `corpus/`. Flag your finding in the commit message either way.

## Commit 1 — artifacts out of VCS (main, safe)

```
git rm -r --cached __pycache__
git rm --cached synthesis-index.json library-index.json extractions-index.json
# append to .gitignore: __pycache__/  *-index.json  generated_seeds/
```
Keep the JSON files on disk (rm --cached only) so the site still builds locally. Verify `node build-synthesis-index.js` (or its npm script) regenerates them before committing.
Message: `chore: remove build artifacts and caches from version control`

## Commit 2 — deployment ephemera → docs/ (main, safe)

Create `docs/`. Collapse, preserving content (concatenate, add dated section headers, light dedupe only — do not rewrite Sam's prose):
- `V2-DEPLOYED.md` + `LIBRARY-V2-CHANGES.md` → `docs/CHANGELOG.md` (newest first)
- `VERCEL-DEPLOYMENT.md` + `DEPLOY-NOW.md` → `docs/deployment.md`
- `LIBRARY-QUICKSTART.md` → `docs/quickstart.md`
- `SYNTHESIS-LIBRARY-COMPLETE.md` + `SYNTHESIS-LIBRARY-README.md` → `docs/synthesis-library.md`
- `CAPSTONE.md` → leave at root (Sam hasn't ruled; flag in PR notes)
Message: `docs: collapse deployment/process records into docs/`

## Commit 3 — corpus/apparatus split (branch only)

`git checkout -b restructure/corpus-apparatus`

Moves (use `git mv` throughout):
- → `corpus/`: synthesis, distillations, correspondences, traditions, protocols, world-tree, world-model, journey, garden, harvest, seeds, memory-palace, negative-space, fiction-bridges, film-slate, voices, extractions, audio-transcripts, translated (+ constellation/ if curated, per pre-flight #4)
- → `apparatus/`: site, server, api, mcp, cli, generation, notebooks (+ client — but see triage below)
- → `apparatus/scripts/`: esoterica.sh, esoterica.bat, launch-library.sh, update-library.sh, build-synthesis-index.js
- Stays at root: README.md, SEED.md, SIGIL.md, INDEX.md, INIT.md, ENGRAMS.md, CLAUDE.md, .claude/, LICENSE, package.json, vercel.json, .gitignore, .env.example, .mcp.json

Triage (investigate, act, note decisions in commit body):
- `client/` vs `apparatus/site/`: if one supersedes the other, keep the live one, move the dead one to `docs/archive/`. Check which vercel.json actually serves.
- `oracle.html`, `oracle.ts` → `apparatus/` if referenced by site/server; else `docs/archive/`.
- `synthesis-library.html`, `synthesis-library-v1.html` → `docs/archive/` if superseded by `site/`.
- `grok-imagine-context.md` → `docs/`.
- `misc/`: promote anything SEED.md links (`misc/TUNING_FORK_PRINCIPLE.md`, `misc/internal/CLAUDE_INITIALIZATION.md`) to paths that keep SEED's links valid (update SEED.md links accordingly); everything else → `corpus/misc/` untouched. Do not delete content without listing it for Sam.

Then patch every path found in pre-flight #3: build script I/O paths, vercel.json, .mcp.json, CLI, shell scripts, any site fetch paths for the index JSONs. Update relative links inside SEED.md / CLAUDE.md / INIT.md that point at moved files. Do NOT rewrite links inside `corpus/` documents in this commit — too broad; instead regenerate INDEX.md in commit 4 and list any broken internal links for Sam.

Verify before committing: index build runs; `vercel build` (or `npx vercel build`) succeeds locally; site loads via local preview and the constellation/library views populate.
Message: `refactor: corpus/ apparatus/ split; path fixes` (+ triage decisions in body)

## Commit 4 — README + INDEX (same branch)

- Replace root `README.md` with `README-draft.md` contents. One required patch: the draft references `apparatus/` and `docs/` — confirm those names survived commit 3 as written.
- Regenerate `INDEX.md` from the new tree (if generation is manual, rewrite it to mirror the new layout at the same depth as the current one).
- Run a link check across root markdown files; fix root-level breakage, report corpus-internal breakage without fixing.
Message: `docs: absent-narrator README; regenerate INDEX for new tree`

## Handback

Open a PR from `restructure/corpus-apparatus` with: the triage decision list, the constellation.json finding, any corpus-internal broken links, and the Vercel preview URL. Sam reviews the preview and merges himself.

## Constraints

- Never edit corpus document contents (typos included) — this is a structural pass only.
- Preserve history: `git mv`, never delete-and-recreate.
- If any verification step fails and the fix isn't obvious within the plan's scope, stop and surface it rather than improvising.
