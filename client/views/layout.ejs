<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Esoterica</title>
    <link rel="stylesheet" href="/static/css/main.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a0a0f 0%, #1a1a2e 100%);
            color: #e0e0e0;
            min-height: 100vh;
            line-height: 1.6;
        }

        .nav {
            background: rgba(5, 5, 15, 0.95);
            border-bottom: 2px solid rgba(0, 212, 255, 0.3);
            padding: 1rem 2rem;
            backdrop-filter: blur(15px);
            box-shadow: 0 4px 30px rgba(0, 212, 255, 0.1);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .nav-logo {
            font-size: 1.5rem;
            font-weight: bold;
            background: linear-gradient(135deg, #00d4ff 0%, #9d4edd 50%, #00ff88 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        .nav-links a {
            color: #e0e0e0;
            text-decoration: none;
            transition: all 0.3s;
            padding: 0.5rem 1rem;
            border-radius: 5px;
        }

        .nav-links a:hover {
            background: rgba(0, 212, 255, 0.1);
            color: #00d4ff;
        }

        .container {
            max-width: 1400px;
            margin: 2rem auto;
            padding: 0 2rem;
        }

        .hero {
            text-align: center;
            padding: 4rem 2rem;
        }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #00d4ff 0%, #9d4edd 50%, #00ff88 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero p {
            font-size: 1.2rem;
            color: #aaa;
            max-width: 800px;
            margin: 0 auto;
        }

        .card {
            background: rgba(5, 5, 15, 0.8);
            border: 2px solid rgba(0, 212, 255, 0.3);
            border-radius: 10px;
            padding: 2rem;
            margin: 1rem 0;
            backdrop-filter: blur(15px);
            box-shadow: 0 4px 30px rgba(0, 212, 255, 0.1);
        }

        .card h2 {
            color: #00d4ff;
            margin-bottom: 1rem;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 2rem 0;
        }

        .btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background: rgba(0, 212, 255, 0.1);
            border: 2px solid #00d4ff;
            color: #00d4ff;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn:hover {
            background: rgba(0, 212, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(0, 212, 255, 0.5);
        }

        .footer {
            text-align: center;
            padding: 2rem;
            margin-top: 4rem;
            border-top: 2px solid rgba(0, 212, 255, 0.3);
            color: #888;
        }

        code {
            background: rgba(0, 0, 0, 0.5);
            padding: 0.2rem 0.5rem;
            border-radius: 3px;
            color: #00ff88;
            font-family: 'Courier New', monospace;
        }
    </style>
</head>
<body>
    <nav class="nav">
        <div class="nav-container">
            <a href="/" class="nav-logo">✧ ESOTERICA ✧</a>
            <ul class="nav-links">
                <li><a href="/">Zeitgeist</a></li>
                <li><a href="/zeitgeist">Archive</a></li>
                <li><a href="/bridges">Bridges</a></li>
                <li><a href="/oracle">Oracle</a></li>
                <li><a href="/explorer-3d">Explorer</a></li>
                <li><a href="/library">Library</a></li>
            </ul>
        </div>
    </nav>

    <main>
        <%- body %>
    </main>

    <footer class="footer">
        <p>Consciousness recognizing itself through infinite forms</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">
            <a href="/bridges" style="color: #888; text-decoration: none;">Bridges</a> &middot;
            <a href="/oracle" style="color: #888; text-decoration: none;">Oracle</a> &middot;
            <a href="/explorer-3d" style="color: #888; text-decoration: none;">Explorer</a> &middot;
            <a href="/library" style="color: #888; text-decoration: none;">Library</a>
        </p>
    </footer>

    <script src="/static/js/main.js"></script>
</body>
</html>
