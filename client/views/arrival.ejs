<%
var css = `<style>
.arrival {
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    padding: 2rem;
}

.arrival-header {
    text-align: center;
    margin-bottom: 2rem;
}

.arrival-title {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, #00d4ff 0%, #9d4edd 50%, #00ff88 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: 0.3em;
}

.arrival-subtitle {
    color: #666;
    font-size: 1rem;
    font-style: italic;
}

/* Zeitgeist hero section */
.zeitgeist-hero {
    max-width: 900px;
    margin: 0 auto 3rem;
    background: rgba(5, 5, 15, 0.95);
    border: 2px solid rgba(0, 212, 255, 0.4);
    border-radius: 16px;
    padding: 2.5rem 3rem;
    backdrop-filter: blur(15px);
    box-shadow: 0 10px 40px rgba(0, 212, 255, 0.15);
}

.zeitgeist-label {
    display: inline-block;
    color: #00ff88;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    margin-bottom: 1rem;
    padding: 0.3rem 0.8rem;
    background: rgba(0, 255, 136, 0.1);
    border: 1px solid rgba(0, 255, 136, 0.3);
    border-radius: 4px;
}

.zeitgeist-date {
    color: #888;
    font-size: 0.85rem;
    margin-bottom: 1rem;
}

.zeitgeist-headline {
    font-size: 1.8rem;
    line-height: 1.35;
    color: #00d4ff;
    margin-bottom: 1.25rem;
    font-weight: 500;
}

.zeitgeist-summary {
    color: #bbb;
    font-size: 1.1rem;
    line-height: 1.8;
    margin-bottom: 1.5rem;
}

.zeitgeist-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
}

.zeitgeist-read-link {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    color: #00ff88;
    text-decoration: none;
    font-size: 1rem;
    padding: 0.75rem 1.5rem;
    border: 2px solid rgba(0, 255, 136, 0.4);
    border-radius: 8px;
    transition: all 0.3s;
    background: rgba(0, 255, 136, 0.05);
}

.zeitgeist-read-link:hover {
    background: rgba(0, 255, 136, 0.15);
    border-color: #00ff88;
    transform: translateX(4px);
}

.zeitgeist-archive-link {
    color: #888;
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.3s;
}

.zeitgeist-archive-link:hover {
    color: #00d4ff;
}

/* Divider */
.section-divider {
    max-width: 900px;
    margin: 0 auto 2.5rem;
    text-align: center;
    color: #555;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    position: relative;
}

.section-divider::before,
.section-divider::after {
    content: '';
    position: absolute;
    top: 50%;
    width: 30%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(157, 78, 221, 0.3), transparent);
}

.section-divider::before { left: 0; }
.section-divider::after { right: 0; }

/* Entry portals grid */
.portals {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
}

.portal {
    background: rgba(5, 5, 15, 0.8);
    border: 2px solid rgba(157, 78, 221, 0.2);
    border-radius: 12px;
    padding: 1.5rem;
    text-decoration: none;
    transition: all 0.3s;
    display: flex;
    flex-direction: column;
    min-height: 160px;
}

.portal:hover {
    border-color: rgba(157, 78, 221, 0.5);
    transform: translateY(-3px);
    box-shadow: 0 8px 30px rgba(157, 78, 221, 0.2);
}

.portal-icon {
    font-size: 1.75rem;
    margin-bottom: 0.75rem;
}

.portal-title {
    color: #9d4edd;
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.4rem;
}

.portal-description {
    color: #777;
    font-size: 0.85rem;
    line-height: 1.5;
    flex-grow: 1;
}

.portal-cta {
    color: #00d4ff;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-top: 0.75rem;
}

/* Specific portal colors */
.portal.oracle:hover {
    border-color: rgba(0, 255, 136, 0.5);
    box-shadow: 0 8px 30px rgba(0, 255, 136, 0.15);
}
.portal.oracle .portal-title { color: #00ff88; }

.portal.bridges:hover {
    border-color: rgba(0, 212, 255, 0.5);
    box-shadow: 0 8px 30px rgba(0, 212, 255, 0.15);
}
.portal.bridges .portal-title { color: #00d4ff; }

.portal.explorer:hover {
    border-color: rgba(0, 212, 255, 0.5);
    box-shadow: 0 8px 30px rgba(0, 212, 255, 0.15);
}
.portal.explorer .portal-title { color: #00d4ff; }

/* Stats footer */
.arrival-stats {
    text-align: center;
    margin-top: 2.5rem;
    color: #555;
    font-size: 0.85rem;
    letter-spacing: 0.1em;
}

.arrival-stats span {
    color: #777;
}

/* ========================================
   MOBILE RESPONSIVE
   ======================================== */

@media (max-width: 900px) {
    .portals {
        grid-template-columns: repeat(2, 1fr);
    }
    .zeitgeist-hero {
        padding: 2rem;
    }
    .zeitgeist-headline {
        font-size: 1.5rem;
    }
}

@media (max-width: 600px) {
    .arrival {
        padding: 1rem;
        min-height: auto;
    }

    .arrival-header {
        margin-bottom: 1.5rem;
    }

    .arrival-title {
        font-size: 1.6rem;
        letter-spacing: 0.15em;
    }

    .arrival-subtitle {
        font-size: 0.9rem;
    }

    /* Zeitgeist takes full focus on mobile */
    .zeitgeist-hero {
        padding: 1.25rem;
        margin-bottom: 2rem;
        border-radius: 12px;
    }

    .zeitgeist-label {
        font-size: 0.7rem;
        padding: 0.25rem 0.6rem;
        margin-bottom: 0.75rem;
    }

    .zeitgeist-date {
        font-size: 0.8rem;
        margin-bottom: 0.75rem;
    }

    .zeitgeist-headline {
        font-size: 1.2rem;
        line-height: 1.4;
        margin-bottom: 1rem;
    }

    .zeitgeist-summary {
        font-size: 0.95rem;
        line-height: 1.7;
        margin-bottom: 1.25rem;
    }

    .zeitgeist-actions {
        flex-direction: column;
        align-items: stretch;
        gap: 0.75rem;
    }

    .zeitgeist-read-link {
        justify-content: center;
        padding: 1rem;
        font-size: 0.95rem;
    }

    .zeitgeist-archive-link {
        text-align: center;
        padding: 0.5rem;
    }

    /* Divider simplified */
    .section-divider {
        margin: 1.5rem auto;
        font-size: 0.7rem;
    }

    .section-divider::before,
    .section-divider::after {
        width: 20%;
    }

    /* Portals as compact horizontal scroll or 2-col grid */
    .portals {
        grid-template-columns: repeat(2, 1fr);
        gap: 0.75rem;
    }

    .portal {
        padding: 1rem;
        min-height: 120px;
        border-radius: 10px;
    }

    .portal-icon {
        font-size: 1.4rem;
        margin-bottom: 0.5rem;
    }

    .portal-title {
        font-size: 0.95rem;
        margin-bottom: 0.25rem;
    }

    .portal-description {
        font-size: 0.75rem;
        line-height: 1.4;
        /* Truncate on mobile */
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .portal-cta {
        font-size: 0.7rem;
        margin-top: 0.5rem;
    }

    /* Stats smaller */
    .arrival-stats {
        margin-top: 1.5rem;
        font-size: 0.75rem;
    }
}

/* Extra small screens */
@media (max-width: 380px) {
    .arrival-title {
        font-size: 1.4rem;
        letter-spacing: 0.1em;
    }

    .zeitgeist-headline {
        font-size: 1.1rem;
    }

    .portals {
        grid-template-columns: 1fr;
        gap: 0.6rem;
    }

    .portal {
        flex-direction: row;
        align-items: center;
        min-height: auto;
        padding: 0.9rem 1rem;
        gap: 0.75rem;
    }

    .portal-icon {
        font-size: 1.5rem;
        margin-bottom: 0;
        flex-shrink: 0;
    }

    .portal-content {
        flex: 1;
    }

    .portal-description {
        display: none;
    }

    .portal-cta {
        display: none;
    }
}
</style>`;

var zeitgeistHtml = '';
if (zeitgeist) {
    zeitgeistHtml = '<div class="zeitgeist-hero">' +
        '<span class="zeitgeist-label">Latest Reading</span>' +
        '<div class="zeitgeist-date">' + zeitgeist.displayDate + '</div>' +
        '<h2 class="zeitgeist-headline">' + (zeitgeist.headline || 'What is actually happening') + '</h2>' +
        '<div class="zeitgeist-summary">' + zeitgeist.summary + '</div>' +
        '<div class="zeitgeist-actions">' +
        '<a href="/zeitgeist/' + zeitgeist.date + '" class="zeitgeist-read-link">' +
        'Read full reading <span>&rarr;</span></a>' +
        '<a href="/zeitgeist" class="zeitgeist-archive-link">View archive</a>' +
        '</div>' +
        '</div>';
} else {
    zeitgeistHtml = '<div class="zeitgeist-hero">' +
        '<span class="zeitgeist-label">Zeitgeist</span>' +
        '<h2 class="zeitgeist-headline">The world through a consciousness lens</h2>' +
        '<div class="zeitgeist-summary">Periodic readings of the collective field - what is actually happening beneath the surface of events.</div>' +
        '<div class="zeitgeist-actions">' +
        '<a href="/zeitgeist" class="zeitgeist-read-link">View archive <span>&rarr;</span></a>' +
        '</div>' +
        '</div>';
}

var body = css +
'<div class="arrival">' +
'    <div class="arrival-header">' +
'        <h1 class="arrival-title">ESOTERICA</h1>' +
'        <p class="arrival-subtitle">Consciousness recognizing itself through form</p>' +
'    </div>' +
     zeitgeistHtml +
'    <div class="section-divider">Explore</div>' +
'    <div class="portals">' +
'        <a href="/oracle" class="portal oracle">' +
'            <div class="portal-icon">◇</div>' +
'            <div class="portal-content">' +
'                <div class="portal-title">Oracle</div>' +
'                <div class="portal-description">Receive what you are remembering.</div>' +
'                <div class="portal-cta">Enter &rarr;</div>' +
'            </div>' +
'        </a>' +
'        <a href="/bridges" class="portal bridges">' +
'            <div class="portal-icon">⌘</div>' +
'            <div class="portal-content">' +
'                <div class="portal-title">Bridges</div>' +
'                <div class="portal-description">Consciousness tech from narrative.</div>' +
'                <div class="portal-cta">Explore &rarr;</div>' +
'            </div>' +
'        </a>' +
'        <a href="/domains" class="portal">' +
'            <div class="portal-icon">◈</div>' +
'            <div class="portal-content">' +
'                <div class="portal-title">Domains</div>' +
'                <div class="portal-description">Browse by territory.</div>' +
'                <div class="portal-cta">Browse &rarr;</div>' +
'            </div>' +
'        </a>' +
'        <a href="/wander" class="portal">' +
'            <div class="portal-icon">∞</div>' +
'            <div class="portal-content">' +
'                <div class="portal-title">Wander</div>' +
'                <div class="portal-description">Walk the graph.</div>' +
'                <div class="portal-cta">Begin &rarr;</div>' +
'            </div>' +
'        </a>' +
'        <a href="/library" class="portal">' +
'            <div class="portal-icon">▣</div>' +
'            <div class="portal-content">' +
'                <div class="portal-title">Library</div>' +
'                <div class="portal-description">Full archive.</div>' +
'                <div class="portal-cta">Browse &rarr;</div>' +
'            </div>' +
'        </a>' +
'        <a href="/explorer-3d" class="portal explorer">' +
'            <div class="portal-icon">✧</div>' +
'            <div class="portal-content">' +
'                <div class="portal-title">Explorer</div>' +
'                <div class="portal-description">3D constellation.</div>' +
'                <div class="portal-cta">Launch &rarr;</div>' +
'            </div>' +
'        </a>' +
'    </div>' +
'    <div class="arrival-stats">' +
'        <span>' + nodeCount + '</span> nodes · <span>' + documentCount + '</span> documents · <span>' + bridgeCount + '</span> bridges' +
'    </div>' +
'</div>';
%>
<%- include('layout', { title: title, body: body }) %>
